// import OutputView from '../views/OutputView.js';

// async function handleInputWithValidator(inputFn, validatorFn, ...args) {
//   try {
//     const input = await inputFn();
//     validatorFn(input, ...args);
//     return await inputFn;
//   } catch (error) {
//     OutputView.printError(error);
//     return handleInputWithValidator(inputFn, validatorFn, ...args);
//   }
// }

// export default handleInputWithValidator;
